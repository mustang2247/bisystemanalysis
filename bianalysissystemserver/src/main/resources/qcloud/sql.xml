<?xml version="1.0" encoding="UTF-8"?>

<commands>
    <!-- param_pattern 一定要和 sql语句中的列数对应上
        param_count 表示param_pattern中问号的个数
        batch_count 表示服务器在缓冲多少条以后集中向数据库发送一次请求
     -->

    <command>
        <op>FishJingJi</op>
        <sql>insert into t_fish_jingji ( timestamp, appid, user_id, user_info, score, other_info ) values</sql>
        <param_pattern>(?,?,?,?,?,?)</param_pattern>
        <param_count>6</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>FishBoss</op>
        <sql>insert into t_fish_boss ( timestamp, boss_id, user_get_gold, boss_blood, level, time, last_hit, other_info)
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>CommonGetItems</op>
        <sql>insert into t_common_get_ietms ( timestamp, appid, user_id, user_info, type, cost_item_id, cost_item_count,
            get_info, other_info, vip_level, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>CommonGrants</op>
        <sql>insert into t_common_mb_grants ( timestamp, appid, user_id, user_info, type, bouns, other_info, vip_level,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>MbFifhItems</op>
        <sql>insert into t_fish_get_ietms ( timestamp, appid, user_id, user_info, type, cost_item_id, cost_item_count,
            get_info, other_info, vip_level, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>MbFifhGrants</op>
        <sql>insert into t_fish_mb_grants ( timestamp, appid, user_id, user_info, type, bouns, other_info, vip_level,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>Handle</op>
        <sql>insert into t_handle ( timestamp, appid, user_id, user_info, handle, other_info, is_mobile ) values</sql>
        <param_pattern>(?,?,?,?,?,?,?)</param_pattern>
        <param_count>7</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>Download</op>
        <sql>insert into t_download ( timestamp, appid, is_wechat, is_android, action,other_info ) values</sql>
        <param_pattern>(?,?,?,?,?,?)</param_pattern>
        <param_count>6</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>FishJackpot</op>
        <sql>insert into t_fish_jackpot( timestamp, appid, user_id, user_info, level, gold, other_info, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>FishPaiment</op>
        <sql>insert into t_fish_paiment( timestamp, appid, user_id, user_info, level, gold, other_info, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>FishItem</op>
        <sql>insert into t_fish_item( timestamp, appid, user_id, user_info, level, item_id, item_count, other_info,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>FishGet</op>
        <sql>insert into t_fish_get( timestamp, appid, user_id, user_info, level, gold, other_info, is_robot, is_mobile
            ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>Exit</op>
        <sql>insert into t_exit( timestamp, appid, user_id, user_info, gold, items, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>KaiFang</op>
        <sql>insert into t_mjdr_kaifang( timestamp, appid, user_id1, user_id2, user_id3, user_id4, user_info, desk_id,
            game_type, fan, jushu, is_huansan, other_info, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>14</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>oneDuoBaoOpen</op>
        <sql>insert into t_duobao_open( timestamp, appid, win_user_id, win_num, act_id,
            item_type, item_id, item_count, need_count, name_info, other_info, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>oneDuoBaoBuy</op>
        <sql>insert into t_duobao_buy ( timestamp, appid, user_id, act_id,
            buy_num, need_count, cost_id, cost_count, other_info, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>ddzChoujiang</op>
        <sql>insert into t_ddz_choujiang( timestamp, appid, user_id, user_info ,
            type, level, num, choujiang_id,item_id, item_count, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>


    <command>
        <op>propUse</op>
        <sql>insert into t_use_props( timestamp, appid, user_id, game_id ,
            type, level, item_id , item_count , use_info , item_source, is_mobile, user_info ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>


    <command>
        <op>ddzMultiple</op>
        <sql>insert into t_ddz_multiple( timestamp,type, level, inning_id, multiple) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>ddzWin</op>
        <sql>insert into t_ddz_win( timestamp, appid, user_id, user_info,
            type, level, inning_id, gold, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>ddzRake</op>
        <sql>insert into t_ddz_rake( timestamp, appid, user_id, user_info,
            type, level, inning_id, gold, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>taskFinish</op>
        <sql>insert into t_task_finish( timestamp, appid, user_id, user_info, game_id, task_id,
            type, level, inning_id, bonus, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>


    <command>
        <op>allOnline</op>
        <sql>insert into t_online( timestamp, appid, user_id, user_info, game_id, kind,
            type, level, inning_id, millis, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>sendControl</op>
        <sql>insert into t_important_props( timestamp, send_appid, get_appid, send_user_id, get_user_id, send_user_info,
            get_user_info, item_id, item_count, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>huafeiControl</op>
        <sql>insert into t_use_huafei( timestamp, appid, user_id, user_info, phone_num, item_id, item_count,card_val,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>costHP</op>
        <sql>insert into t_mjdr_hp( timestamp, appid, user_id, user_info, type, count, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>winStreak</op>
        <sql>insert into t_mjdr_win_streak( timestamp, appid, user_id, user_info, level, game_type, num, get_info,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>actPeople</op>
        <sql>insert into t_activity_people( timestamp, appid, user_id, activity_id, is_mobile ) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>newactivity</op>
        <sql>insert into t_newactivity( timestamp, appid, user_id, activity_id, cost_id, cost_count, get_info, is_mobile
            ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updateDAU</op>
        <sql>insert into t_dau( timestamp, appid, user_id, ip, platform, creative, market_info, is_ccgames, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updateInstall</op>
        <sql>insert into t_install( timestamp, appid, user_id, open_id, inviter_id, from_where, ip, remark, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updatePay</op>
        <sql>insert into t_pay( timestamp, appid, user_id, user_info, item_id, item_buy_count, item_count, platform,
            order_info, money, money2, money3, ip, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>14</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updateGrants</op>
        <sql>insert into t_grants( timestamp, appid, user_id, user_info, type, item_id, item_count, gold, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updateGrants2</op>
        <sql>insert into t_grants2( timestamp, appid, user_id, user_info, type, item_id, item_count, gold, exp, hp,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>13</param_count>
        <batch_count>1</batch_count>
    </command>


    <command>
        <op>updatePossession</op>
        <sql>insert into t_possession( timestamp, appid, user_id, user_info, gold, items, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>updateActivity</op>
        <sql>insert into t_activity( timestamp, appid, user_id, user_info, activity_id, sub_id, cost_id, cost_count,
            got_id, got_count, detail, is_ccgames, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>13</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>missionFinish</op>
        <sql>insert into t_mission_finish( timestamp, appid, user_id, user_info, type, gold, is_ccgames, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>missionFinish2</op>
        <sql>insert into t_mission_finish2( timestamp, appid, user_id, user_info, type, gold, exp, hp, item_id,
            item_count, is_ccgames, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>exchangeItems</op>
        <sql>insert into t_exchange( timestamp, appid, user_id, user_info, cost_id, cost_count, item_id, item_name,
            item_count, real_name, mobile_no, addr, other_info, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>14</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrInning</op>
        <sql>insert into t_mjdr_inning( timestamp, type, level, inning_id ) values</sql>
        <param_pattern>(?,?,?,?)</param_pattern>
        <param_count>4</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrPlay</op>
        <sql>insert into t_mjdr_play( timestamp, appid, user_id, user_info, type, level, inning_id, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrWin</op>
        <sql>insert into t_mjdr_win( timestamp, appid, user_id, user_info, type, level, inning_id, gold, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrRake</op>
        <sql>insert into t_mjdr_rake( timestamp, appid, user_id, user_info, type, level, inning_id, gold, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrUpgrade</op>
        <sql>insert into t_mjdr_upgrade( timestamp, appid, user_id, user_info, levels, gold, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrStat</op>
        <sql>insert into t_mjdr_stat( timestamp, appid, user_id, user_info, type, level, inning_id, zimo, dianpao,
            minggang, angang, fan, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>15</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrOnline</op>
        <sql>insert into t_mjdr_online( timestamp, appid, user_id, user_info, type, level, inning_id, millis,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrHandle</op>
        <sql>insert into t_mjdr_handle( timestamp, appid, user_id, user_info, type, level, inning_id, handle_type,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mjdrLottery</op>
        <sql>insert into t_mjdr_lottery( timestamp, appid, user_id, user_info, level, lottery_level, inning_id,
            cost_score, own_score, bonus_id, bonus_count, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>14</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskPlay</op>
        <sql>insert into t_gdesk_play( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskBet</op>
        <sql>insert into t_gdesk_bet( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskWin</op>
        <sql>insert into t_gdesk_win( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskPump</op>
        <sql>insert into t_gdesk_pump( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskJackpot</op>
        <sql>insert into t_gdesk_jackpot( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            cards, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskWinDetail</op>
        <sql>insert into t_gdesk_win_detail( timestamp, winner_id, loser_id, desk_id, desk_type, inning_id, chips )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?)</param_pattern>
        <param_count>7</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskCounter</op>
        <sql>insert into t_gdesk_counter( timestamp, desk_id, desk_type, inning_id, duration, detail ) values</sql>
        <param_pattern>(?,?,?,?,?,?)</param_pattern>
        <param_count>6</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>gdeskExpense</op>
        <sql>insert into t_gdesk_expense( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, kind,
            item_id, item_count, chips, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>14</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>getMasterPoint</op>
        <sql>insert into t_mp_get( timestamp, appid, user_id, user_info, master_point, source, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>costMasterPoint</op>
        <sql>insert into t_mp_cost( timestamp, appid, user_id, user_info, master_point, item_id, item_count, addr,
            is_ccgames, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbBet</op>
        <sql>insert into t_zfb_bet( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join, ratio,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbWin</op>
        <sql>insert into t_zfb_win( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbPump</op>
        <sql>insert into t_zfb_pump( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbJackpot</op>
        <sql>insert into t_zfb_jackpot( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbZhuang</op>
        <sql>insert into t_zfb_shangzhuang( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_use_card, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbXiaZhuang</op>
        <sql>insert into t_zfb_xiazhuang( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_use_card, is_escape, escape_pump, use_card_change, ratio, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>16</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbFlowers</op>
        <sql>insert into t_zfb_flowers( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_use_card, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbFreeRed</op>
        <sql>insert into t_zfb_free_reward( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_use_card, is_free, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>13</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbSendExp</op>
        <sql>insert into t_zfb_send_expression( timestamp, appid, chips, user_id, user_info, inning_id, banker_id,
            is_join, is_use_card, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbZhuangPump</op>
        <sql>insert into t_zfb_zhuang_pump( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_join,
            is_use_card, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>zfbGetRed</op>
        <sql>insert into t_zfb_get_red( timestamp, appid, chips, user_id, user_info, inning_id, banker_id, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mtl2Bet</op>
        <sql>insert into t_mtl2_bet( timestamp, appid, user_id, user_info, bet_gold, bet_nb_gold, bet_xnb_gold,
            bet_info, win_gold, jackpot, challenge_rank, challenge_win, inning_type, inning_id, champion, multiple,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>19</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>mtl2Pump</op>
        <sql>insert into t_mtl2_pump( timestamp, type, gold ) values</sql>
        <param_pattern>(?,?,?)</param_pattern>
        <param_count>3</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>forestDanceBet</op>
        <sql>insert into t_mtl_bet( timestamp, appid, user_id, user_info, inning_id, chips, multiple, button,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>forestDanceWin</op>
        <sql>insert into t_mtl_win( timestamp, appid, user_id, user_info, inning_id, chips, multiple, button,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>forestDanceJackpot</op>
        <sql>insert into t_mtl_jackpot( timestamp, appid, user_id, user_info, inning_id, chips, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>forestDancePump</op>
        <sql>insert into t_mtl_pump( timestamp, appid, hour_of_day, minute, chips ) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>fruitMachineBet</op>
        <sql>insert into t_fruit_bet( timestamp, appid, user_id, user_info, inning_id, chips, idx, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>fruitMachineWin</op>
        <sql>insert into t_fruit_win( timestamp, appid, user_id, user_info, inning_id, chips, winkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>fruitMachineJackpot</op>
        <sql>insert into t_fruit_jackpot( timestamp, appid, user_id, user_info, inning_id, chips, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>fruitMachinePump</op>
        <sql>insert into t_fruit_pump( timestamp, appid, hour_of_day, minute, chips ) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>caiBeiBet</op>
        <sql>insert into t_caibei_bet( timestamp, appid, user_id, user_info, guess_type, ratio, chips, card, times,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>caiBeiWin</op>
        <sql>insert into t_caibei_win( timestamp, appid, user_id, user_info, guess_type, ratio, chips, card, times,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>caiBeiPump</op>
        <sql>insert into t_caibei_pump( timestamp, appid, hour_of_day, minute, chips ) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>caiBeiItemReward</op>
        <sql>insert into t_caibei_reward( timestamp, appid, user_id, user_info, times, item_id, item_count, level,
            total_worth, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>runHallBet</op>
        <sql>insert into t_ppt_bet( timestamp, appid, user_id, user_info, inning_id, chips, betkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>runHallWin</op>
        <sql>insert into t_ppt_win( timestamp, appid, user_id, user_info, inning_id, chips, winkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>runHallJackpot</op>
        <sql>insert into t_ppt_jackpot( timestamp, appid, user_id, user_info, inning_id, chips, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>runHallSSCBet</op>
        <sql>insert into t_pptssc_bet( timestamp, appid, user_id, user_info, inning_id, chips, betkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>runHallSSCWin</op>
        <sql>insert into t_pptssc_win( timestamp, appid, user_id, user_info, inning_id, chips, winkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>dogSportsMeetingBet</op>
        <sql>insert into t_dogsport_bet( timestamp, appid, user_id, user_info, inning_id, chips, betkey, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>dogSportsMeetingWin</op>
        <sql>insert into t_dogsport_win( timestamp, appid, user_id, user_info, inning_id, chips, winner_id, multiple,
            road_idx, is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>12</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>dogSportsMeetingJackpot</op>
        <sql>insert into t_dogsport_jackpot( timestamp, appid, user_id, user_info, inning_id, chips, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>dogSportsMeetingPump</op>
        <sql>insert into t_dogsport_pump( timestamp, appid, hour_of_day, minute, chips ) values</sql>
        <param_pattern>(?,?,?,?,?)</param_pattern>
        <param_count>5</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmTicket</op>
        <sql>insert into t_farm_ticket( timestamp, appid, user_id, user_info, cost, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmLandExtension</op>
        <sql>insert into t_farm_landext( timestamp, appid, user_id, user_info, cost, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmSystemSell</op>
        <sql>insert into t_farm_sell( timestamp, appid, user_id, user_info, item_id, item_count, price, total,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmStartMining</op>
        <sql>insert into t_farm_mining( timestamp, appid, user_id, user_info, producer_type, ground_idx, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmDiamondProduce</op>
        <sql>insert into t_farm_produce( timestamp, appid, user_id, user_info, amount, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmDiamondExchange</op>
        <sql>insert into t_farm_exchange( timestamp, appid, price, amount ) values</sql>
        <param_pattern>(?,?,?,?)</param_pattern>
        <param_count>4</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>farmDiamondExchangePump</op>
        <sql>insert into t_farm_exchange_pump( timestamp, appid, user_id, user_info, pump, buyer_id, order_id,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>yylBet</op>
        <sql>insert into t_yyl_bet( timestamp, appid, user_id, user_info, chips, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>8</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>yylWin</op>
        <sql>insert into t_yyl_win( timestamp, appid, user_id, user_info, type, chips, is_ccgames, is_robot, is_mobile )
            values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>timeluckBet</op>
        <sql>insert into t_timeluck_bet( timestamp, appid, user_id, user_info, type, chips, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>timeluckWin</op>
        <sql>insert into t_timeluck_win( timestamp, appid, user_id, user_info, type, chips, is_ccgames, is_robot,
            is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>scmjPlay</op>
        <sql>insert into t_scmj_play( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>scmjWin</op>
        <sql>insert into t_scmj_win( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>scmjPump</op>
        <sql>insert into t_scmj_pump( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>thirteenPlay</op>
        <sql>insert into t_thirteen_play( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>thirteenWin</op>
        <sql>insert into t_thirteen_win( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>thirteenPump</op>
        <sql>insert into t_thirteen_pump( timestamp, appid, user_id, user_info, desk_id, desk_type, inning_id, chips,
            is_ccgames, is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>11</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>ronglianDrop</op>
        <sql>insert into t_ronglian_drop( timestamp, appid, user_id, user_info, type, item_id, item_count, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>ronglianJoin</op>
        <sql>insert into t_ronglian_join( timestamp, appid, user_id, user_info, item_id, item_count, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>9</param_count>
        <batch_count>1</batch_count>
    </command>

    <command>
        <op>grabRed</op>
        <sql>insert into t_grab_red( timestamp, appid, user_id, user_info, inning_id, round_id, gold, is_ccgames,
            is_robot, is_mobile ) values
        </sql>
        <param_pattern>(?,?,?,?,?,?,?,?,?,?)</param_pattern>
        <param_count>10</param_count>
        <batch_count>1</batch_count>
    </command>

</commands>